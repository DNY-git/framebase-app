# API origin used for full-page navigations that can't go through a proxy
# (e.g. the Google OAuth redirect). Production value on Vercel:
# https://framebase-app.onrender.com
# Leave unset for local dev — paths stay relative and use the Vite proxy.
VITE_API_URL=
