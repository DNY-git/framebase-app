{
  "name": "constructtrack",
  "version": "0.1.0",
  "private": true,
  "description": "Production-grade Construction Tracking Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/types && npm run build --workspace=packages/ui && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=packages/config && npm run lint --workspace=packages/types && npm run lint --workspace=packages/ui && npm run lint --workspace=apps/api && npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=packages/types && npm run typecheck --workspace=packages/ui && npm run typecheck --workspace=apps/api && npm run typecheck --workspace=apps/web",
    "test": "npm run test --workspace=packages/types && npm run test --workspace=apps/api && npm run test --workspace=apps/web",
    "seed": "npm run seed --workspace=apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "allowScripts": {
    "@nestjs/core": true,
    "esbuild": true,
    "mongodb-memory-server": true
  }
}
