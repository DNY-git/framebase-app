{
  "name": "@constructtrack/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI component library for ConstructTrack",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'UI build will be configured in Phase 2'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'No lint for ui package placeholder'",
    "test": "echo 'No tests for ui package placeholder'"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
