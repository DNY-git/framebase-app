# ConstructTrack — Fix Equipment Detail, Reports KPIs, Material Transactions, and Dashboard Layout Issues

You are continuing work on my existing **ConstructTrack** application (NestJS + MongoDB Atlas backend, Vite/React frontend, monorepo).

Do NOT rebuild the project or replace existing architecture. Work with the current codebase, existing API contracts, database models, UI components, theme, and Bklit UI/chart implementation — the same rules as the previous task apply.

Before changing anything, inspect the relevant frontend and backend files and understand how the current implementation works.

The screenshots I provided show the current problems.

---

## 1. REMOVE THE UTILISATION COLUMN FROM THE EQUIPMENT LIST

Current UI (equipment list row):

```text
EQUIPMENT | TYPE | STATUS | PURCHASE DATE | PURCHASE COST | UTILISATION
```

The `UTILISATION` column is not needed in the list view.

### What I need

* Remove the `UTILISATION` column from the equipment list table.
* Do not remove utilization data from the backend/model — only stop showing it in this list view, unless it is unused elsewhere, in which case leave the underlying data/computation alone regardless.
* Confirm whether utilization is still shown on the equipment detail screen and leave that as-is unless it's broken (see Section 2).

---

## 2. FIX THE EQUIPMENT DETAIL SCREEN VALIDATION ERROR + MAKE IT THE EDIT SCREEN

Opening an equipment item currently shows a themed detail screen (dark background, equipment name, cost, serial, utilization/hours/downtime stats, maintenance timeline, usage timeline, downtime section) — but it also shows a raw error banner:

```json
{"statusCode":400,"errorCode":"VALIDATION_ERROR","message":"Validation failed","errors":[{"field":"property","constraint":"INVALID","message":"property perPage should not exist"}],"requestId":"...","timestamp":"..."}
```

### What I need

Trace the complete request that populates this screen (likely usage/maintenance/downtime timeline queries):

```text
Equipment Detail Screen
        ↓
Frontend API request (query params, e.g. perPage)
        ↓
Controller
        ↓
Query DTO / class-validator decorators
        ↓
Service
        ↓
MongoDB / Mongoose
```

Determine exactly why `perPage` is rejected — most likely the global `ValidationPipe` has `whitelist`/`forbidNonWhitelisted` enabled and the query DTO for this endpoint does not declare a `perPage` field, while the frontend is still sending it.

### Important

Do NOT just remove validation or strip the param client-side to silence the error without understanding it.

* If `perPage` is a real, intended parameter (e.g. for paginating the usage/maintenance/downtime timelines), add it properly to the query DTO with correct validation decorators.
* If `perPage` is a leftover/unused parameter the frontend shouldn't be sending, remove it from the frontend request instead.
* Never show raw JSON error payloads in the UI — display a proper themed error state consistent with the rest of the app if a real error occurs.

### Also: make this the edit screen, not a popup

Currently there appears to be a separate pop-up/modal used for editing equipment that does not match the app's theme (looks unstyled/out of place, like a generic editing form).

* Instead of that modal popping out, tapping/clicking an equipment row in the list (Section 1) should navigate to this existing Equipment Detail screen.
* Add editing capability to the Equipment Detail screen itself (e.g. an "Edit" action that lets the user update name, serial number, category, purchase date, purchase cost, etc., using the same validated contract from the previous Equipment task).
* Remove or retire the mismatched popup once the detail screen covers editing, unless it's used elsewhere for a legitimately different purpose — investigate before deleting anything.
* Keep the existing dark theme, layout, typography, and card structure of the detail screen; do not redesign it.

### Acceptance criteria

* Opening any equipment item shows the detail screen with no validation error banner.
* Tapping an equipment row navigates to the detail screen (no themed-inconsistent popup).
* The detail screen supports editing equipment fields using the existing validated contract.
* List/detail/edit/delete equipment functionality continues working end-to-end.

---

## 3. REPLACE THE REPORTS SECTION KPI CARDS WITH A COMPACT STAT ROW

The Reports section currently shows four large KPI cards:

```text
[icon] 2   Report templates
[icon] 2   Report runs
[icon] 2   Succeeded
[icon] 0   Failed
```

I want this replaced with a compact horizontal stat style, like this existing pattern elsewhere in the app:

```text
(1) In Progress     (0) Blocked     (0) Done
```

### What I need

* Find where this compact stat-row pattern (small numbered badge + bold label, inline, no large cards) is already implemented elsewhere in the app and reuse that existing component/style — do not build a new one from scratch if a reusable component already exists.
* Apply this same compact pattern to the Reports section's existing metrics: Report templates, Report runs, Succeeded, Failed.
* Keep the underlying data/counts as they are — this is a visual/layout change, not a data change.
* Match existing theme (dark/light), spacing, and typography conventions used by the compact stat row elsewhere.

### Acceptance criteria

* The Reports section shows the four existing metrics in the compact horizontal style instead of the large card grid.
* Counts are still accurate and pulled from the same existing data source.
* No other report functionality (templates, runs, etc.) is broken.

---

## 4. ADD THE MATERIAL CATALOG TO THE TRANSACTION LEDGER + FIX THE ERROR

The "Record Transaction" section (material ledger) currently has:

* A "Select material" dropdown with no predefined options apparent
* A red banner: `An unexpected error occurred. Retry`
* "No transactions found" below it

### What I need

**A. Apply the same catalog concept already built for Equipment** (from the previous task: seeded catalog collection, `GET` catalog endpoint, "select existing item" + "+ Add new" UX) to Materials in this transaction form:

* Investigate the existing Materials/Inventory domain (should already exist per the previous task's catalog work) and reuse it — do not create a duplicate model.
* The "Material" dropdown in "Record Transaction" should let the user pick from the existing seeded material catalog, with an option to add a new material if it's not in the catalog, following the same pattern as the equipment catalog picker.
* Keep the distinction between a catalog item/type and an actual transaction record (type, quantity, project ID, cost, note) — same architecture pattern used for Equipment (catalog item → actual asset/instance).

**B. Fix the "An unexpected error occurred" banner:**

Trace the request that loads this transaction ledger view:

```text
Transaction Ledger UI
        ↓
Frontend API request (materials list / transactions list, filters)
        ↓
Controller
        ↓
Service
        ↓
MongoDB / Mongoose
```

Find and fix the actual root cause — do not just swallow the error or replace the message. If it's a genuinely empty dataset, that should render as "No transactions found" without an error banner, not both at once.

### Important

Do not hardcode fake materials or fake transactions. Do not bypass validation. Do not hide the underlying error instead of fixing it.

### Acceptance criteria

* The Material dropdown in Record Transaction offers the seeded catalog + "add new material" option.
* The red "unexpected error occurred" banner no longer appears under normal operation.
* An empty transaction list renders as a clean "No transactions found" state only.
* Recording a transaction (receive/issue/etc.) against a catalog material works end-to-end.

---

## 5. DASHBOARD — VERIFY SPENDING LINE, CONVERT PROJECT PROGRESS TO A BAR CHART, FIX OVERLAP

### A. Verify the previously implemented spending line

The "Spending vs Budget" chart should already have both a `Budget` and `Spent` line from the previous task. I don't need it rebuilt — I need you to **verify it is actually working** (real data, correct legend, correct tooltip) and report back on its current state as part of your final report. Do not change this chart unless you find it's genuinely broken.

### B. Convert "Project Progress" to a bar chart

"Project Progress" currently shows only an empty state:

```text
Project Progress
Active projects by timeline progress

[icon]
No active projects
Projects will appear here when started.
```

* Investigate how "Project Progress by timeline" data is computed/retrieved elsewhere in the app (it's referenced in the dashboard already, so the aggregation likely exists or is planned).
* Replace the panel's populated-state rendering with a **bar chart** showing active projects' timeline progress (e.g. one bar per active project, percentage complete).
* Use an existing shadcn/ui chart component for this (the app already uses shadcn/ui elsewhere per the existing stack) — do not introduce an unrelated charting library if shadcn's chart primitives can do this.
* **Keep the existing empty state** ("No active projects" / "Projects will appear here when started.") when there genuinely are no active projects — only replace what renders when there is data to show.
* Match the existing dashboard's dark theme, card style, and typography.

### C. Fix the overlapping layout issue

The "Spending vs Budget" card and the "Project Progress" card currently overlap/collide at certain viewport sizes (visible in the screenshot). Find the root cause in the grid/flex layout (likely a fixed width/height, missing responsive breakpoint, or z-index/positioning issue) and fix the underlying layout, not just visually nudge one element.

### Acceptance criteria

* Spending vs Budget chart confirmed working (or, if actually broken, fixed) and reported on.
* Project Progress renders as a bar chart when active projects exist, using shadcn/ui.
* Project Progress still shows the correct empty state when there are no active projects.
* The two dashboard cards no longer overlap at any normal viewport size.
* No other dashboard sections (Project Activity, other widgets) are removed or redesigned.

---

## 6. IMPLEMENTATION REQUIREMENTS

Before making changes:

1. Inspect the repository.
2. Identify the existing Equipment list/detail/edit components and API.
3. Identify the existing Materials/Inventory catalog domain built in the previous task.
4. Identify the existing Reports section and its KPI components, and the existing compact stat-row component used elsewhere (e.g. Tasks status row).
5. Identify the Transaction Ledger frontend and backend (materials, transactions, list/create endpoints).
6. Identify the Dashboard implementation, the Bklit UI Spending vs Budget chart, and the Project Progress panel.
7. Identify existing shadcn/ui chart components already available in the project.
8. Identify existing DTOs, schemas, enums, and validation relevant to each area above.
9. Identify existing tests.

Do not create duplicate functionality that already exists.

---

## 7. TEST EVERYTHING

After implementation, run the appropriate project checks:

```bash
npm run typecheck
npm test
npm run lint
```

Use the actual scripts defined in the repository if the names differ.

Also manually test through the API/UI where appropriate.

### Equipment

```text
List equipment (no UTILISATION column)
Open equipment detail (no validation error banner)
Edit equipment from the detail screen
Confirm popup-based editing is gone or no longer the primary path
```

### Reports

```text
Reports section shows compact stat row with correct counts
```

### Materials / Transactions

```text
Material dropdown shows catalog items + "add new material"
Record a transaction against a catalog material
Confirm no "unexpected error occurred" banner on load
Empty state renders cleanly when there are no transactions
```

### Dashboard

```text
Spending vs Budget — confirm real Budget + Spent data (report status)
Project Progress — bar chart renders for active projects
Project Progress — empty state still correct with no active projects
No overlap between Spending vs Budget and Project Progress cards
```

---

## 8. IMPORTANT CODING RULES

Do not:

* rewrite the application
* replace the existing architecture
* remove validation
* hardcode fake materials, transactions, or dashboard data
* create duplicate catalog/domain models unnecessarily
* replace Bklit UI or introduce a new charting library for the Spending vs Budget chart
* remove existing functionality
* modify unrelated pages
* hide errors instead of fixing them
* show raw JSON error payloads in the UI

Prefer:

* existing components (especially the compact stat-row and the equipment/material catalog pattern)
* existing services and API patterns
* existing DTOs and database models
* existing design tokens and theme
* existing shadcn/ui components for the new Project Progress bar chart
* existing testing patterns

---

## 9. FINAL DELIVERABLE

When finished, give me a concise implementation report containing:

### Fixed

* Utilisation column removed from equipment list
* Equipment detail validation error (`perPage should not exist`)
* Equipment editing moved from popup to detail screen
* Reports KPI cards replaced with compact stat row
* Material catalog added to Record Transaction
* Transaction ledger "unexpected error occurred" issue
* Project Progress converted to bar chart (shadcn/ui)
* Dashboard card overlap issue

### Status check

* Confirmation (with evidence, not assumption) of whether the Spending vs Budget "Spent" line from the previous task is actually working with real data.

### Files changed

List the important files changed and explain what each change does.

### Tests

Show the commands run and their results.

### Remaining issues

Only list genuine remaining issues. Do not claim something is fixed unless you actually tested it.

Finally, create a git commit for these changes using a clear conventional commit message, for example:

```text
fix: equipment detail screen, reports KPIs, material catalog, and dashboard layout
```

Use the repository's existing git conventions if they differ.

Do not stop after identifying the problem. **Implement the fixes, test them, and commit the completed work.**