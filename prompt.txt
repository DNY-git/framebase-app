# FrameBase — Fix Render Deploy: Port Binding and Google OAuth Callback URL

You are continuing work on the existing FrameBase application. Do NOT rebuild the project or replace existing architecture.

The app builds successfully on Render but fails to actually come up as a live service, for two separate reasons visible in the deploy logs:

```text
==> No open ports detected, continuing to scan...
==> Docs on specifying a port: https://render.com/docs/web-services#port-binding
```

and

```text
[GOOGLE-DIAG] callbackUrl = http://localhost:4000/api/v1/auth/google/callback
```

(Atlas's TLS connection rejection is being handled separately by adding Render's IP to Atlas Network Access — you don't need to touch that part.)

---

## 1. BIND TO RENDER'S ASSIGNED PORT, NOT A HARDCODED ONE

Render assigns a port dynamically via the `PORT` environment variable at runtime and expects the app to bind to it. Right now the app appears to be listening on a hardcoded port (implied to be `4000`, based on the OAuth callback URL), which means Render never detects the service as "up," even if everything else works.

### What I need

* Find where the NestJS app calls `app.listen(...)` (in `apps/api/src/main.ts` or equivalent).
* Make sure it reads the port from `process.env.PORT`, falling back to a sensible local-dev default only when `PORT` isn't set, e.g.:
  ```ts
  const port = process.env.PORT ? Number(process.env.PORT) : 4000;
  await app.listen(port);
  ```
* Confirm this doesn't break local development — locally, `PORT` typically isn't set, so it should still fall back to whatever port (`4000` or otherwise) the app currently uses for local dev.
* Search the codebase for any other place that might hardcode a port (e.g. a separate health-check server, a WebSocket gateway, CORS config referencing a specific port) and fix those too if found.

### Acceptance criteria

* The app binds to `process.env.PORT` when it's set (as it will be on Render), and to the existing local default when it isn't.
* No other part of the app assumes a fixed port that would conflict with this.

---

## 2. FIX THE GOOGLE OAUTH CALLBACK URL FOR PRODUCTION

The Google OAuth strategy is currently using a hardcoded `http://localhost:4000/api/v1/auth/google/callback` callback URL, which will not work once deployed (Google will redirect to a URL that doesn't exist in production, and Google's OAuth client also needs to have the exact production URL authorized).

### What I need

* Find the Google OAuth strategy setup (likely `GoogleStrategy` in a Passport config file) and confirm where `callbackURL` is currently being set.
* Change it to read from an environment variable instead of a hardcoded value, e.g.:
  ```ts
  callbackURL: process.env.GOOGLE_CALLBACK_URL || 'http://localhost:4000/api/v1/auth/google/callback',
  ```
  (keep the localhost value only as the local-dev fallback default, matching whatever the app currently uses for local dev).
* Confirm the app reads `GOOGLE_CALLBACK_URL` (or whatever name fits the existing env var naming convention in this project — check for consistency with other `GOOGLE_*` vars already in use) cleanly, with no trailing typos/slashes.
* Do not commit any real production URL into the code — it should come entirely from the environment variable, since I'll set the actual value in Render's dashboard.

### Acceptance criteria

* The Google OAuth callback URL is driven by an environment variable in production, with the existing localhost URL preserved as the local-dev default.
* Local development continues to work unchanged.

---

## 3. VERIFICATION

```bash
npm run typecheck
npm test
npm run lint
```

Confirm the app still starts correctly in local dev after these changes (i.e. `PORT` unset locally still results in the app listening on its usual local port, and Google login still works locally against the unchanged localhost callback URL).

Since actually confirming this fixes the Render deploy requires re-deploying (which I'll do myself after you push these changes), be explicit in your report about exactly what you changed and why you're confident it addresses both log errors.

---

## 4. FINAL DELIVERABLE

Give me a concise report containing:

### Fixed

* App now binds to `process.env.PORT` in production, with local-dev fallback preserved
* Google OAuth callback URL now driven by an environment variable, with local-dev fallback preserved
* Any other hardcoded port/URL references found and fixed

### Files changed

List the important files changed and what each change does.

### What I still need to do manually

* Set `GOOGLE_CALLBACK_URL` (or the actual variable name used) in Render's environment variables to the real production URL
* Add that same production callback URL to the Google Cloud Console OAuth client's authorized redirect URIs
* Confirm Render's IP is allowed in MongoDB Atlas Network Access (0.0.0.0/0)

### Tests

Commands run and results.

Commit the completed work with a clear conventional commit message, e.g.:

```text
fix: bind to Render's dynamic port and use env-driven Google OAuth callback URL
```

Do not stop after identifying the problem. **Implement the fixes and commit the completed work.**