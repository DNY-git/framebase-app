# FrameBase — Enforce Real Secrets in Production, Fix Stale Bootstrap Log

You are continuing work on the existing FrameBase application. Do NOT rebuild the project or replace existing architecture.

The app is now successfully deployed and live on Render. Two things stood out in the production startup logs:

```text
warn: Insecure default secrets detected: JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, PASSWORD_PEPPER, COOKIE_SECRET. Acceptable in development only. {"context":"ConfigValidationService"}
...
info: ConstructTrack API running on http://localhost:4000 [development] {"context":"Bootstrap"}
```

---

## 1. CONFIRM/ENFORCE THAT PRODUCTION CANNOT RUN WITH DEFAULT SECRETS

There's already a `ConfigValidationService` that detects insecure default secrets (`JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `PASSWORD_PEPPER`, `COOKIE_SECRET`) and logs a warning saying this is "acceptable in development only" — but it currently only warns, it doesn't appear to block startup in production.

### What I need

* Find `ConfigValidationService` and see exactly what it currently does when it detects a default/insecure secret, and how it determines the current environment (`NODE_ENV` or equivalent).
* Confirm: does it currently just log a warning in every environment, or does it already throw/exit when `NODE_ENV=production` and a default secret is detected? Report which of these is actually true right now.
* If it does NOT currently block startup in production, add that enforcement: when running in production with any of these four secrets still set to their known default/placeholder value, the app should refuse to start and throw a clear, actionable error (e.g. "Refusing to start in production with a default JWT_ACCESS_SECRET. Set a real value in your environment.") rather than just logging a warning and continuing.
* Do not change this behavior for non-production environments (development/test should keep working with default secrets, unchanged).
* Do not weaken or remove the existing warning for non-production — only add the production-blocking behavior.

### Acceptance criteria

* Running with `NODE_ENV=production` and any of the four default secrets still in place causes the app to fail to start with a clear error message, instead of just logging a warning and continuing to run insecurely.
* Development/test behavior is unchanged.

---

## 2. FIX THE BOOTSTRAP LOG MESSAGE

The startup log currently prints:

```text
ConstructTrack API running on http://localhost:4000 [development]
```

even when actually running in production on Render. This is misleading on two counts: the old "ConstructTrack" name (the app was renamed to FrameBase), and it always says `localhost:4000` and `[development]` regardless of the real environment/port.

### What I need

* Find this log line (likely in `main.ts`'s `Bootstrap` logger call) and:
  * Update "ConstructTrack API" to "FrameBase API" (or whatever the app's actual current name/branding is — check for a shared app-name constant/config value used elsewhere, e.g. in the frontend's `APP_NAME`, and reuse it here if one already exists, rather than hardcoding a second copy of the name).
  * Make the printed URL and environment label reflect reality: use the actual bound port (`process.env.PORT` when set, matching the earlier port-binding fix) and the actual `NODE_ENV` value, rather than hardcoded `localhost:4000` and `[development]`.
* Confirm this still makes sense for local development (i.e. it should still correctly print `localhost:<port> [development]` when running locally with no `PORT`/`NODE_ENV=production` set).

### Acceptance criteria

* The bootstrap log line says "FrameBase API" (or the correct current app name), and accurately reflects the real port and environment the app is actually running in, both locally and in production.

---

## 3. VERIFICATION

```bash
npm run typecheck
npm test
npm run lint
```

Also confirm locally that:
* Running with `NODE_ENV=production` and default secrets still in place causes a clear startup failure (test this locally by temporarily setting `NODE_ENV=production` in your sandbox, if your sandbox can run the app at all — otherwise describe exactly how I should test this myself before redeploying).
* Running normally in development still starts up fine with the existing default secrets and correct log output.

---

## 4. FINAL DELIVERABLE

Give me a concise report containing:

### Fixed

* Production startup now refuses to run with default/insecure secrets (or confirmation that this was already enforced, if it turns out it was)
* Bootstrap log message now shows the correct app name, real port, and real environment

### Files changed

List the important files changed and what each change does.

### Tests

Commands run and results, including how you verified the production-secret-blocking behavior.

### Important — before I redeploy

Remind me explicitly: if this now blocks production startup on default secrets, I must set real values for `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `PASSWORD_PEPPER`, and `COOKIE_SECRET` in Render's environment variables **before** redeploying, or the next deploy will fail to start on purpose.

Commit the completed work with a clear conventional commit message, e.g.:

```text
fix: enforce real secrets in production, correct bootstrap log message
```

Do not stop after identifying the problem. **Implement the fixes and commit the completed work.**